# Supabase (client-side, dibatasi RLS — anon key, bukan service role key)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Mapbox GL JS
NEXT_PUBLIC_MAPBOX_TOKEN=

# URL backend FastAPI (dipakai frontend untuk memanggil endpoint laporan/rute)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
